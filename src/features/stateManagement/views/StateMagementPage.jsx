import React from 'react';
import { Typography } from '@material-ui/core';

const StateMagementPage = () => {
  return (
    <React.Fragment>
      <Typography variant="h5">State magement page</Typography>
    </React.Fragment>
  );
};

export default StateMagementPage;
