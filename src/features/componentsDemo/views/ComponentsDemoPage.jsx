import React from 'react';
import { Typography } from '@material-ui/core';

const ComponentsDemoPage = () => {
  return <Typography variant="h5">Components demo page</Typography>;
};

export default ComponentsDemoPage;
